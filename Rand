#include <iostream>
#include <time.h>
using namespace std;
int main() {
    int a , b[100];
    cout << "Geben Sie ein Nummer: ";
    cin >> a;
    srand(time(NULL));
    for (int i = 0; i <a; ++i) {
        b[i]=rand()%101;
        cout << b[i] << "\t";
    }
    return 0;
}
